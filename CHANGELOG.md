# CHANGELOG.md — История версий CLAUDE.md

> Часть проекта Bitcoin-Intel. Основной файл: [CLAUDE.md](CLAUDE.md)

---

| Версия | Дата | Изменения |
|--------|------|-----------|
| v8.13 | 2026-07-20 | Новая база `MAJOR_DRAWDOWNS.json` — 6 крупнейших исторических просадок цены BTC (2011 Mt.Gox-взлом, 2013 апрельский пузырь, 2013-2015 Китай+банкротство Mt.Gox, 2017-2018 ICO-пузырь, 2021-2022 Terra/FTX, 2025-2026 текущая — не завершена). Тот же принцип, что ENTITIES.json/REVENUE_ENGINES.json — застывшие факты, не сигналы. При исследовании найдено и явно задокументировано (не выбрано молча) существенное расхождение источников по апрельскому 2013 краху: большинство ретроспективных статей дают $266→$50 (−81…−84%), но первоисточник — статья Bitcoin Magazine от 14 апреля 2013, написанная по горячим следам — фиксирует внутридневной минимум $105 (~−60% в моменте); зафиксировано поле `source_agreement` на каждую запись. Попутно (в рамках этой же сессии, отдельным разговором) исправлена неверная дата в пользовательском вопросе про '1 цент 15 июля 2010' — по Clark Moody ('Ten Years of Bitcoin Market Data') и независимому подтверждению историка Пита Риццо, верная дата — 8 октября 2010 (обвал с ~10 центов до ATL $0,01, не 'достижение' как рост) |
| v8.12 | 2026-07-19 | Новый кластер `bitcoin_governance_debate` (theme: narrative) — первый сигнал NAR-2026-0711-001 (Сэйлор против BIP-110). Ни `theme`, ни субъект ни одного из 6 существующих кластеров не совпали — первая тема в базе про протокольное управление/консенсус. Четыре места одновременно: `signals.json`, `ontology.json.clusters`, `DIGEST_CLUSTER_LABELS` и `CLUSTER_LABELS` (index.html) — таблица «Кластеры (текущие)» синхронизирована тем же коммитом (тест-страж v8.8 иначе упал бы). |
| v8.11 | 2026-07-19 | Реализованы оба предложения из обсуждения машиночитаемости CLAUDE.md. `schemas/signal/v1.json`: 8 enum-полей (`narrative_role`, `theme`, `weight`, `dir`, `horizon`, `cat`, `actor`, `flow`) переведены с голого `enum` на `oneOf` с `const`+`description` на каждое значение — смысл каждого значения (ранее существовавший только в прозе CLAUDE.md) теперь доступен любому инструменту, читающему только схему. Формат эквивалентен `enum` для валидации (проверено `jsonschema.Draft7Validator`: 0 новых ошибок на всех 75 реальных сигналах, невалидные значения по-прежнему отклоняются) — поведение не изменилось. `tests/unit/test_claude_md_schema_sync.py` расширен 10 тестами: дословная сверка description в схеме с соответствующими таблицами CLAUDE.md (5 полей общей формы «Значение\|Смысл» + отдельные парсеры для `theme`/`cat`/`narrative_role`, у которых другая форма таблицы), явная проверка что все 8 полей используют `oneOf` (не откатились на `enum`), страж-на-стража. Проверено на реальной инъекции — искажённое описание прямо в файле схемы ловится тестом, после отката снова проходит. Добавлена одна строка-указатель в раздел «Справочник значений полей» о существовании этой machine-readable копии. 279 тестов всего (269→279) |
| v8.10 | 2026-07-19 | Реализованы обе отложенные из v8.9 части («Желательно» + «Опционально»). Раздел FACTS «Текущий охват» переформулирован: числа-счётчики убраны из прозы вовсе (они протухали при каждом facts-сигнале — v8.9 актуализировала их вручную в третий раз за миграцию, четвёртый был бы неизбежен), список сущностей сохранён (полезен для быстрой проверки «уже отслеживается?»). `tests/unit/test_facts.py` расширен тестом `test_claude_md_facts_coverage_matches_data`: сверяет список из прозы CLAUDE.md с фактическими ключами `data/facts.json`, раздельно для индивидуальных сущностей и агрегатных категорий — граница между ними выводится программно (по наличию `entity_id` в `ENTITIES.json`), не хардкодится, по формуле уже задокументированной в разделе «Формат ключа». Проверено на реальном регрессионном сценарии в сессии: добавление фиктивного факта без соответствующей строки в CLAUDE.md ловится тестом (не только на синтетическом юнит-кейсе). 269 тестов всего (267→269). **Версионное напоминание сессии: +0.4 с v8.6 — см. финальное сообщение.** |
| v8.9 | 2026-07-19 | Найдено при повторном, более осторожном аудите CLAUDE.md (по внешней спеке с явным запретом менять то, что уже хорошо спроектировано — audit-first, категории Обязательно/Желательно/Опционально): раздел FACTS «Текущий охват» содержал два устаревших числа. (1) «10 ключей, 9 сущностей» — фактически 12 ключей: `spacex.btc_holdings` добавлен в данные, но не упомянут в перечислении вовсе. (2) «~15 сущностей ENTITIES.json» без миграции на FACTS — фактически ENTITIES.json вырос до 36 записей (9 уже мигрировано), не ~24 на момент написания оригинальной цифры. Оба числа — не гипотетический риск, проверены напрямую по `data/facts.json`/`ENTITIES.json` и были неверны прямо на момент аудита. Классифицировано как «Обязательно» (документ содержал фактически ложное утверждение) — исправлено. Остальные 5 пунктов аудита (архитектура директорий, полнота 3 JSON Schema, ADR-покрытие, доменная документация BDKS/BAMS/NIES/DATA_DICTIONARY/ALGORITHM, размер файла 598 строк) подтверждены уже соответствующими практике — изменений не потребовалось, полный рефакторинг по внешней спеке отклонён как несоразмерный вторично. 267 тестов (без изменений — правка только текстовая, тест на этот конкретный счётчик отнесён к категории «Опционально», не выполнен без отдельного подтверждения) |
| v8.8 | 2026-07-19 | Найдено при разборе внешнего документа-предложения о полной реорганизации CLAUDE.md в модульный LLM Specification Pack (schemas/rules/algorithms/docs/ADR): большая часть предложенной модульности уже существует (16 ADR в `docs/`, формальные JSON Schema в `schemas/`, `test_claude_md_schema_sync.py`) — полный рефакторинг признан несоразмерным текущему состоянию (598 строк — уже в целевом диапазоне 300–700) и рискованным (конвертация прозы в формальную схему без эквивалента посимвольной сверки, которая была возможна для SIGNALS.md). Применена только точечная, обоснованная часть: таблица «Кластеры (текущие)» дублирует `ontology.json.clusters` (сам файл называет его источником истины) без теста-стража — тот же класс дрейфа, что FACTS/SITE_MAP/SIGNALS.md. Обнаружен и исправлен реальный дрейф: описание `supply_scarcity` в таблице потеряло ", цикличность" по сравнению с ontology.json. `tests/unit/test_claude_md_schema_sync.py` расширен тестом `test_claude_md_cluster_table_matches_ontology_json` (двусторонняя сверка ключей + дословное совпадение описаний), проверен на 3 подставных сценариях регрессии (искажённое описание / устаревшая строка / новый кластер без документации). 267 тестов всего |
| v8.7 | 2026-07-19 | `SIGNALS.md` переведён с ручного ведения на механизм — `scripts/build_signals_md.py` (детерминированный генератор из `signals.json`) + `tests/unit/test_signals_md_sync.py` (байт-в-байт страж, 6 тестов). Найдено при плановой уборке: правило «два файла одновременно» действовало без механизма проверки — за несколько месяцев последние ~12 сигналов дописывались в конец файла в произвольном порядке вместо заявленной сортировки (`date` DESC, тай-брейк `id` DESC — выведен эмпирически из ещё не разъехавшейся части файла). Регенерация вскрыла и содержательный дрейф текста, не только порядка: 10 сигналов имели непустой `alternatives_considered` в `signals.json`, который старый ручной `.md` никогда не рендерил; несколько блоков `data`/`context`/`caveat` отстали от более новых правок в JSON. Попутно найден и исправлен реальный баг данных: `NAR-2026-0702-001.data` содержал усечённое `~1 937` вместо `~$61 937` (проверено по контексту сигнала и независимо подтверждено старым текстом `.md`) — единственный случай за проверку 13 похожих по форме чисел, остальные оказались легитимными величинами BTC, не ценами. Тот же класс проблемы и то же решение, что уже применялись для FACTS (v8.2, AD-6) и SITE_MAP (v8.5) — процедура без механизма проверки в этом проекте не держится. Таблица файлов проекта и правило «Сигнал: схема объекта» обновлены, чтобы не противоречить уже существовавшей (но нерабочей) декларации файла «регенерируется из него». 266 тестов всего |
| v8.6 | 2026-07-13 | «Ключевые тренды» (вкладка ХОЛДЕРЫ) переведены с захардкоженного массива текста на вычисляемую логику: `computeHolderTrend()` строит описание тренда (первая точка → пик → последняя точка, либо простой рост без пика) напрямую из `HOLDERS_DATA.snapshots` при каждой отрисовке. Найдено: старый массив ссылался на данные Oct 2024 и молча разошёлся с реальностью после обновления снэпшота до Jul 2026 — теперь такое расхождение структурно невозможно, панель наследует любое обновление снэпшота автоматически. Проведена сверка всех текущих `facts.json` (top100_public_companies, etf_exchanges и др.) против категорий снэпшота — новых числовых коррекций не потребовалось, но выводы сверки зафиксированы в комментариях `data/holders.js` явно (согласуется/не сравнимо и почему), а не оставлены непроверенными. Документирован статус «Ключевые тренды» как downstream от правила верификации HOLDERS_DATA — отдельно поддерживать панель не нужно |
| v8.5 | 2026-07-12 | SITE_MAP — карта структуры сайта. Проблема: за сессию несколько находок о дублировании/устаревании (график эмиссии дублировал 3 места, панель уезжала не в тот кластер, футер не обновлялся из-за неправильного триггера вкладки) обнаруживались случайно — не было машиночитаемой карты структуры сайта. Добавлено: `data/site_map.json` (82 записи, ручной инвентарь всех панелей 4 кластеров/13 вкладок) — на каждую запись кластер/вкладка/тип/происхождение данных (`live`/`facts`/`static`/`signals`/`none`/`tool`)/ключевые слова. `tests/unit/test_site_map_sync.py` — сверяет манифест с `index.html` в ОБЕ стороны (запись без соответствия в HTML = переименовали/удалили панель, не обновив манифест; panel-title в HTML без записи = забыли занести новую панель). UI: кнопка 🔍 в шапке → полноэкранный оверлей с поиском (по названию/ключевым словам/вкладке) и деревом кластер→вкладка→запись с цветным бейджем происхождения данных; клик — переход на вкладку + скролл к панели по точному тексту заголовка (не по id — не разбросано на полсотни мест). 244 теста всего |
| v8.4 | 2026-07-11 | FACTS Фаза 5 — сторожевой аудит против повторного дрейфа. `scripts/check_stale_facts.py`: берёт полную историю значений каждого отслеживаемого ключа (`build_facts.superseded_values()`, новая функция — не только текущее по `as_of`, а все когда-либо встреченные), ищет устаревшие значения голым текстом в `index.html`/`TREASURY_HOLDERS.json` вне `data-fact-key` (там placeholder безопасен) и вне JS-констант fallback (`const NAME = число;`, явно исключены с комментарием, не молча). Оформлен как pytest-тест — попадает в основной CI-прогон автоматически, не требует отдельного подключения в workflow. Проверен на подставленном кейсе, что детектор реально ловит, не просто проходит. При первом прогоне сразу нашёл забытое место (`mstr-live-btc` обновлялся отдельным JS-механизмом напрямую по id, не через `data-fact-key`) — унифицировано на единую конвенцию вместо добавления исключения. 238 тестов всего |
| v8.3 | 2026-07-11 | Продолжение миграции FACTS (Фаза 4). Добавлены facts для 6 сущностей: `twenty_one_capital`, `mara_holdings`, `bstr` (в существующий STR-2026-0703-001), `el_salvador` (STR-2026-0701-002), `oranjebtc` (STR-2026-0706-002), `strive` (STR-2026-0706-003) — итого 10 ключей, 9 сущностей. `scripts/build_facts.py` расширен: теперь также синхронизирует `TREASURY_HOLDERS.json.holders[].btc` по явному маппингу имён (`TREASURY_NAME_TO_FACT_KEY`) — обнаружено и исправлено 3 реальных расхождения при первом прогоне (Strategy 847363→843775, Strive 19864→19882, OranjeBTC 3898→3904), залогированы явно (`::warning::`), не тихая правка. Добавлен универсальный HTML-механизм `applyFactsToDOM()` — любой элемент с `data-fact-key="entity_id.metric"` подхватывает значение автоматически, без кода под каждый случай миграции; уже применён к 7 местам (Strategy ×6, Metaplanet ×1) взамен устаревшего захардкоженного текста. Футер «Скорость сокращения эмиссии» переведён на детерминированный JS-расчёт по текущей высоте блока (не через facts — протокольная константа, см. «Что НЕ проходит через этот механизм») — было статичным текстом с данными Oct 2024, молча разошедшимся с реальностью. Тесты `sync_treasury_holders()` добавлены в `tests/unit/test_facts.py` (233 теста всего). Итог: 11 реальных расхождений найдено и устранено с начала миграции |
| v8.2 | 2026-07-11 | Реализован механизм FACTS — целевая архитектура для устранения расхождений между `signals.json` (единственный источник истины по BDKS/NIES) и производными поверхностями (`ENTITIES.json.profile.metrics`, захардкоженные числа в `index.html`), нарушавшими уже декларированный принцип «производные ссылаются, не копируют» (NIES S12). Конкретный подтверждённый повод: `signals.json.meta.strategy_btc_reserve` (847 363, вручную обновлено 25 июня) разошёлся с фактом из сигнала STR-2026-0706-001 (843 775, 5 июля) незамеченным. Добавлено: опциональное поле `facts` в схему сигнала (`schemas/signal/v1.json`) — ключ `entity_id.metric` + `{value, unit, as_of}`; `scripts/build_facts.py` — резолвит последнее по `as_of` значение на ключ, пишет производный `data/facts.json` (никогда не редактируется руками, только регенерируется — как `SIGNALS.md`); `.github/workflows/build-facts.yml` — автоперегенерация при изменении `signals.json`, паттерн branch+PR+auto-merge (как `update-volume.yml`); `tests/unit/test_facts.py` — тест-страж написан в этом же PR, не постфактум (урок AD-6): проверяет уникальность ключей внутри сигнала и корректность resolve-логики независимо от порядка файла. Наполнено 4 тестовых ключа (`strategy.btc_holdings`, `strategy.usd_reserve`, `top100_public_companies.btc_holdings`, `etf_exchanges.btc_holdings`) — оба типа (сущность из ENTITIES.json / агрегатная категория без сущности) намеренно проверены за один заход. Первая реальная миграция: `fetchMstrData()` теперь приоритетно читает резерв BTC Strategy из `window.FACTS`, не из устаревающего `meta.strategy_btc_reserve`. Новый раздел «FACTS — машиночитаемые факты» в CLAUDE.md. Полная миграция остальных захардкоженных чисел `index.html` — предстоящая постепенная работа, не завершена этим коммитом |
| v8.1 | 2026-07-11 | Добавлен `data/holders.js` в таблицу файлов проекта (существовал в репозитории, но не был задокументирован в CLAUDE.md с момента появления). Новый раздел «HOLDERS_DATA — правило верификации»: если в ходе обработки сигнала встречается свежая цифра по сумме владения любой из 6 категорий (individuals/lost_satoshi/governments/companies/etfs/not_mined) — данные проверяются и снэпшот обновляется/добавляется. Задокументирована обнаруженная методология датасета — `individuals` всегда residual (21M минус остальные категории), не независимо измеренная величина — так устроены все прошлые снэпшоты, новые обязаны сохранять эту же логику. Добавлена таблица приоритетных источников по категориям (BitcoinTreasuries.net для companies/ETFs, Arkham для governments и т.д.). Практический повод: составлен снэпшот "Jul 2026" из нескольких источников на разные даты (Newhedge/MacroMicro для not_mined, MEXC для lost_satoshi/companies/etfs, Arkham via River/KuCoin для governments) — конфликт источников по governments (305k vs 432k у MEXC) зафиксирован в комментарии к записи, не разрешён молчаливым выбором |
| v8.0 | 2026-07-07 | Создан новый кластер `leverage_deleveraging_cycle` (первый сигнал: `MAC-2026-0706-001`, теория domain-владельца — не форсировать в существующий `etf_institutional_flow` при несовпадении субъекта). При создании обнаружено и исправлено двойное расхождение документация↔код, державшееся с v7.1 (2026-07-03) незамеченным: (1) инструкция «создание кластера = два места одновременно» была неверна — мест фактически четыре, не два: `signals.json` + `ontology.json.clusters` (реальный источник истины, на который ссылается `schemas/signal/v1.json`, но который CLAUDE.md не упоминал вообще) + `DIGEST_CLUSTER_LABELS` (index.html, короткие подписи) + `CLUSTER_LABELS` (index.html, отдельный словарь для карточек «Главные нарративы» — не упоминался с момента введения правила в v7.1). Обновлены: таблица «Кластеры (текущие)», «Правило: изменения index.html» (исключение расширено на оба словаря), пункт «Создание кластера = четыре места одновременно» |
| v7.9 | 2026-07-05 | Закрытие AD-7 (`docs/NIES.md` v2.0→v2.1) в пользу D13 (Immutability Policy, `BLUEPRINT_ADDENDUM.md` §16.4): убрана инструкция «переписать поле `tension` у нужного сигнала» — она напрямую противоречила формально принятому правилу «tension immutable после первого синтеза». Единственный путь смены tension кластера теперь — новый сигнал с более сильным `tension` и честно набранным `contradicts` (второй путь уже существовал как основной, первый был лишней и опасной альтернативой). Изменено: Шаг 5 (правило `contradicts`), «Нарративный синтез» → «Правило управления tension кластера». Решение принято владельцем домена по прямому запросу — выбран вариант (а) из трёх, предложенных при регистрации AD-7 |
| v7.8 | 2026-07-05 | Реализация AD-6 (`docs/NIES.md` v1.4→v1.5): в раздел «Изменение этого файла» добавлен пункт 4 — изменение состава полей примера схемы сигнала требует синхронного обновления `properties` в `schemas/signal/v1.json` тем же коммитом. Добавлен тест-страж `tests/unit/test_claude_md_schema_sync.py`, автоматически сверяющий поля примера CLAUDE.md со схемой на каждом PR (по образцу `test_ontology_settings_consistency.py`/M3) — расхождение теперь падает в CI на PR со спекой, не постфактум на первом сигнале с новым полем. Повод: паттерн повторился дважды (`alternatives_considered` v7.6, `alternative_scenario` v7.7) — оба раза схема правилась реактивно после падения Contract Tests на реальном сигнале (последний раз — INF-2026-0702-001, PR #130) |
| v7.7 | 2026-07-04 | AD-4 частично закрыт (`docs/ADR-015-alternative-scenario-partial-closure.md`): добавлено опциональное поле `alternative_scenario` в схему сигнала (заполняется при невысокой уверенности, качественная оценка аналитика). Отбор и показ сценария в карточке кластера отложен — зависит от AD-1. Обновлены: JSON-схема, таблица Шага 3, инструкция внутри Шага 3, чеклист Шага 7. Также в этой сессии: AD-1 в NIES переформулирован после обнаружения `ADR-011`/`RR-06` (`docs/IRP_v1.md`) — калибровка confidence уже осознанно отложена проектом независимо от NIES, статус изменён с «требует формализации» на «не закрывать до триггера 30 синтезов» |
| v7.6 | 2026-07-03 | Закрыт архитектурный долг AD-2 (NIES): добавлено поле `alternatives_considered` в схему сигнала — отклонённые на Шаге 3 альтернативные интерпретации теперь фиксируются в данных, не только в показанном рассуждении чата. Обновлены: JSON-схема, таблица маппинга Шага 3, инструкция внутри Шага 3, чеклист Шага 7. Действует только вперёд — 52 существующих сигнала не мигрируются ретроактивно |
| v7.5 | 2026-07-03 | Секция «Проект» сокращена до позиционирования + ссылок. Убрано описание конвейера сигналы→кластеры→нарративы и иерархии уровней BDKS→BAMS→NIES→ALGORITHM — эти детали дублировали уже существующие специализированные секции («Кластеры», «Сигнал: схема объекта», «Нарративный синтез») |
| v7.4 | 2026-07-03 | Переписана секция «Проект»: «образовательный ресурс» → «Narrative Intelligence Platform» с компактным описанием конвейера (сигналы → кластеры → нарративы) и иерархии уровней BDKS→BAMS→NIES→ALGORITHM→реализация. Попутно уточнена формулировка долга AD-3 в docs/NIES.md: golden-регрессия существует для синтезатора (пакетный контур), отсутствует именно для валидации изменений методологии против прецедентов BDKS Р13 |
| v7.3 | 2026-07-03 | Добавлен `docs/NIES.md` v1.0 — Narrative Intelligence Engine Specification: логическая архитектура системы, выведенная из BDKS v1.2 + BAMS v1.2. 12 подсистем (S1–S12), каталог 18 требований, информационные потоки (A1–A9), жизненные циклы, архитектурные контракты, матрицы трассировки BDKS→NIES / BAMS→NIES, отчёт валидации. Закрывает разрыв между BAMS (метод) и ALGORITHM.md (алгоритмы): NIES ссылается на ALGORITHM.md как действующий Reasoning-слой. Расхождения реализации с идеалом зафиксированы как архитектурный долг AD-1…AD-5 (явное поле уверенности, след отвергнутых гипотез, Golden Dataset-регрессия и др.) |
| v7.2 | 2026-07-03 | Добавлен `REVENUE_ENGINES.json` — база механизмов монетизации корпоративных/государственных BTC-держателей (дивиденды, options-доход и т.п.), дополняет `ENTITIES.json`. Файл добавлен в таблицу проекта. Наполнен двумя кейсами (Strategy STRC, Metaplanet Bitcoin Income Generation). Добавлен блок «Доходные движки» в index.html (#tab-tech, рядом с «Экосистема Bitcoin») и CI-шаг валидации JSON |
| v7.1 | 2026-07-03 | Задокументирована ранее скрытая связка cluster↔index.html: создание нового кластера требует строки в `DIGEST_CLUSTER_LABELS` (index.html, renderSignals()), иначе фильтр показывает сырой SNAKE_CASE — тихо нарушало правило «index.html не трогать». Добавлено явное исключение из этого правила + пункт в чеклист Шага 8. Зафиксирован факт: catLabel в текущем рендере используется только как недостижимый fallback (реальную подпись даёт DIGEST_CLUSTER_LABELS по cluster) — поле остаётся обязательным как контракт данных, не как то что реально рисуется |
| v7.0 | 2026-07-03 | Полный аудит и реструктуризация файла. Устранены дубли: правила tension/macro_implication/narrative_role — канон в Шаге 6; тест contradicts + запрет накрутки влиты в Шаг 5; таблицы префиксов и theme объединены; правила версионирования слиты в один раздел. Синхронизация: score-таблица приведена к ALGORITHM.md v2.0 (tension не в score; contradicts +5 за id), убраны битые якоря на несуществующие разделы ALGORITHM.md, убраны устаревшие счётчики кластеров (счёт по signals.json). Канонизирован catLabel (капс, 1 метка на cat). Новые правила: неизменность id, catLabel и ENTITIES в чеклисте Шага 8 |
| v7.0 | 2026-07-03 | Полный аудит и реструктуризация файла + данных. CLAUDE.md: устранены дубли правил (tension/macro_implication/narrative_role/contradicts-тест/версионирование — каждое теперь в одном месте); score-таблица синхронизирована с ALGORITHM.md v2.0 (tension не участвует в score; contradicts +5 за id); убраны 2 битых якоря на несуществующие разделы ALGORITHM.md; убраны устаревшие счётчики кластеров (были неверны — счёт теперь по signals.json); canonical catLabel-таблица (1 метка на cat). Данные: catLabel нормализован в 31/49 сигналов (было 12 вариантов на 6 категорий); INF-2026-0625-002 cat narrative→ownership (контент не соответствовал категории); SIGNALS.md полностью регенерирован из signals.json — было 4 разных формата записи, 1 дублирующийся id-заголовок, 1 сломанный заголовок, покрытие 28/49 сигналов → стал 1 формат, 49/49, 0 дублей |
| v6.1 | 2026-07-02 | Документы знания переведены на неверсионированные имена (docs/BDKS.md, docs/BAMS.md; версия в шапке) — устранение churn ссылок при минорных версиях. Обновлена ссылка в примечании о депрекации `ta`. Таблица файлов проекта дополнена: BDKS, BAMS, DATA_DICTIONARY |
| v6.0 | 2026-07-02 | Депрецирована категория `ta`: технический анализ выведен за границы предметной области решением владельца (BDKS v1.1, Раздел 1 / CR-14 в docs/BDKS_v1.1_Change_Log.md). Исторических сигналов с категорией нет |
| v5.9 | 2026-06-30 | Новое правило видимости рассуждения: Шаги 3–6 (интерпретационные решения) показываются пользователю до сборки JSON, а не только в финальной сводке. Инцидент: разбор сигнала OranjeBTC (STR-2026-0630-001) не был показан до записи |
| v5.8 | 2026-06-30 | Шаг 3: формализовано правило определения `date` — берётся из самого проверенного источника, а не выводится расчётом от относительных формулировок пользователя («вчера»/«сегодня») относительно даты сессии. Инцидент: STR-2026-0629-002 ошибочно датирован 29-м вместо 30-го |
| v5.7 | 2026-06-30 | Исправлены битые ссылки на `ALGORITHM.md` (был в корне, теперь `docs/`) и `STRUCTURE.md` (был в корне, теперь `archive/`) в таблице файлов проекта и в трёх местах раздела алгоритма синтеза |
| v5.6 | 2026-06-30 | Шаг 8 (Чеклист перед записью): добавлена ссылка на commit-конвенцию `signal:` из CONTRIBUTING.md (N3 ARR v3 — устранена несостыковка между файлами) |
| v5.2 | 2026-06-28 | Правило управления tension кластера в ALGORITHM.md и CLAUDE.md |
| v5.1 | 2026-06-27 | Добавлен ALGORITHM.md — документация нарративного синтеза. Раздел алгоритма в CLAUDE.md. |
| v5.0 | 2026-06-26 | База артефактов ENTITIES.json: типы, схема объекта, правила обновления. Файл добавлен в таблицу проекта |
| v4.9 | 2026-06-26 | Правило сводки после записи сигнала: формат, правила вывода links, расшифровка narrative_role |
| v4.8 | 2026-06-26 | Исправлены 5 структурных проблем: цикл в шаге 4, неполный маппинг шага 3, правило id в шаге 8, чеклист записи, правило наполнения вкладок встроено в шаг 1 |
| v4.7 | 2026-06-26 | Алгоритм обработки сигнала: три разрозненных блока свёрнуты в единый линейный pipeline из 8 шагов. Уточнены критерии связывания confirms/contradicts/context_chain. Синтез вынесен после связывания |
| v4.4 | 2026-06-23 | Правило версионного напоминания: триггер на +0.4 за сессию |
| v4.3 | 2026-06-23 | `etf_institutional_flow` → 5 сигналов (GBTC outflow) |
| v4.2 | 2026-06-23 | `etf_institutional_flow` → 4 сигнала (BITA) |
| v4.1 | 2026-06-23 | Новый кластер `btc_treasury_competition`: корпоративное накопление BTC за пределами Strategy. Обновлена таблица кластеров |
| v4.0 | 2026-06-23 | Система связывания сигналов: поля `id`, `links`, `narrative_role`, `cluster`, `tension`, `macro_implication`. Алгоритм анализа связей. Таблица кластеров. Типы связей: confirms / contradicts / context_chain |
| v3.2 | 2026-06-22 | Добавлены `actor` и `flow` в схему сигнала |
| v3.0 | 2026-06-22 | Полная ревизия: убраны дублирования, добавлена таблица файлов проекта, signals.json как единственное место для сигналов, cat-коды в таблице категорий |
| v2.3 | 2026-06-22 | Правило сигналов: signals.json вместо index.html |
| v2.2 | 2026-06-21 | Добавлена категория 🏛️ Владение |
| v2.1 | 2026-06-20 | Правило двойной записи сигналов: index.html + SIGNALS.md |
| v2.0 | 2026-06-20 | Рефакторинг структуры: вынесены SIGNALS.md, STRUCTURE.md, CHANGELOG.md |
| v1.0 | 2026-06-20 | Первая версионированная редакция |
