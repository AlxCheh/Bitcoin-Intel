# CLAUDE.md — Инструкции для работы с проектом

> **Версия:** v4.7 · **Обновлено:** 2026-06-26
> История версий → [CHANGELOG.md](CHANGELOG.md)

---

## Проект

Образовательный ресурс о Bitcoin. **Только Bitcoin — никаких других тем.**
Сайт: https://alxcheh.github.io/Bitcoin-Intel · Репозиторий: github.com/AlxCheh/Bitcoin-Intel

---

## Файлы проекта

| Файл | Назначение |
|------|-----------|
| `index.html` | Единственный файл сайта. Не трогать для сигналов |
| `signals.json` | База сигналов. Только сюда добавлять новые сигналы |
| `SIGNALS.md` | Текстовая документация сигналов (читаемый архив) |
| `STRUCTURE.md` | Архитектура, дизайн-система, навигация |
| `CHANGELOG.md` | История версий CLAUDE.md |

---

## Добавление сигнала

Каждый новый сигнал — **два файла одновременно**: `signals.json` + `SIGNALS.md`.

### Полная схема объекта сигнала

```json
{
  "id": "STR-2026-0623-001",
  "date": "YYYY-MM-DD",
  "cat": "ownership",
  "catLabel": "🏛️ ВЛАДЕНИЕ",
  "dir": "neg",
  "horizon": "mid",
  "theme": "institutionalization",
  "weight": "media",
  "actor": "corporate",
  "flow": "outflow",
  "signal": "Заголовок сигнала",
  "data": ["метрика 1", "метрика 2"],
  "context": "Исторический контекст и аналогии.",
  "caveat": "Риски и ограничения интерпретации.",
  "source": "Источник (месяц год)",
  "links": {
    "confirms": ["id или заголовок сигнала который этот усиливает"],
    "contradicts": ["id или заголовок сигнала с которым в противоречии"],
    "context_chain": ["id сигнала из контекста которого вырастает этот"]
  },
  "narrative_role": "complication",
  "cluster": "strategy_model_stress",
  "tension": "краткое описание противоречия которое сигнал раскрывает",
  "macro_implication": "что означает на уровне общей картины"
}
```

> `index.html` для сигналов не трогать — данные загружаются из `signals.json` через fetch.

---

## Метаметки сигнала

**`id`** — уникальный идентификатор по формату `PREFIX-YYYY-MMDD-NNN`:

| Префикс | Тема |
|---------|------|
| `STR` | institutionalization |
| `SUP` | supply |
| `INF` | infrastructure |
| `MAC` | macro |
| `NAR` | narrative |

**`dir`** — что сигнал говорит о силе Bitcoin как актива в своём горизонте:

| Значение | Смысл |
|----------|-------|
| `pos` | усиливает позицию BTC |
| `neg` | создаёт давление на BTC |
| `neu` | структурный факт без направления |

**`horizon`** — временной горизонт:

| Значение | Смысл |
|----------|-------|
| `short` | недели–месяцы |
| `mid` | месяцы–год |
| `long` | годы, структурные изменения |

**`theme`** — смысловой кластер:

| Значение | Смысл |
|----------|-------|
| `supply` | дефицит, эмиссия, потерянные монеты |
| `institutionalization` | ETF, корпорации, правительства, владение |
| `infrastructure` | Lightning, Layer 2, майнинг |
| `macro` | ставки, ликвидность, макро-давление |
| `narrative` | философия, регуляция, нарративы |

**`weight`** — достоверность источника:

| Значение | Смысл |
|----------|-------|
| `onchain` | верифицируемые данные блокчейна |
| `market` | биржевые данные, цена, объёмы |
| `media` | медиа, аналитики, оценки |
| `primary` | первичный источник (высказывание, документ) |

**`actor`** — субъект сигнала:

| Значение | Смысл |
|----------|-------|
| `etf` | биржевые фонды (BlackRock, Fidelity) |
| `corporate` | публичные компании (Strategy, MSTR) |
| `government` | государственные резервы |
| `defi` | протоколы, on-chain структуры |
| `retail` | розничные инвесторы |
| `miner` | майнеры |

**`flow`** — направление капитала:

| Значение | Смысл |
|----------|-------|
| `inflow` | капитал входит в BTC |
| `outflow` | капитал уходит из BTC |
| `internal` | перераспределение внутри структуры |
| `neutral` | движения капитала нет или неизвестно |

---

## Категории сигналов

| cat | catLabel | Примеры |
|-----|----------|---------|
| `onchain` | 📊 On-chain | Production Cost, MVRV, NVT |
| `ta` | 📈 Технический анализ | Уровни, паттерны, тренды |
| `macro` | 🏦 Макроэкономика | Ставки, ликвидность, DXY |
| `mining` | ⛏️ Майнинг | Хешрейт, сложность, капитуляция |
| `narrative` | 📰 Нарратив | Регуляция, институционалы, события |
| `layer2` | 🔗 Layer 2 | Stacks, Lightning, сайдчейны |
| `ownership` | 🏛️ Владение | Распределение по типам владельцев |

---

## Кластеры (текущие)

| cluster | Описание | Сигналов |
|---------|----------|---------|
| `strategy_model_stress` | Устойчивость модели Strategy под давлением долга | 7 |
| `etf_institutional_flow` | Потоки капитала через ETF и институциональные структуры | 5 |
| `btc_treasury_competition` | Корпоративное накопление BTC выходит за пределы Strategy | 2 |
| `btc_infrastructure_growth` | Рост инфраструктуры: Lightning, L2, транзакции | 3 |
| `supply_scarcity` | Дефицит предложения: потерянные монеты, эмиссия | 1 |

> Новый кластер создаётся когда появляется 2+ сигнала с общим `tension` которого нет в существующих.

---

## Алгоритм обработки сигнала

### Шаг 1 — Приём материала

Определить формат входящего материала: текст, ссылка, скриншот.
Зафиксировать что именно прислал пользователь — это исходная точка, не интерпретация.

---

### Шаг 2 — Исследование

Найти первоисточник:
- Если дана ссылка → `fetch` по ней
- Если нет → `web_search` по ключевым фактам материала

Найти 2–3 дополнительных источника по типу сигнала:

| Тип сигнала | Источники |
|------------|-----------|
| Корпоративные покупки BTC | Arkham, SEC filing, пресс-релиз |
| ETF / регуляторика | SEC.gov, Bloomberg ETF, проспект |
| Майнинг / инфраструктура | Hashrate Index, Glassnode, отчёты |
| Макро / on-chain | Glassnode, CryptoQuant, VanEck ChainCheck |
| Нарративы / медиа | Bitcoin Magazine, CoinDesk, The Block |

- Если источники противоречат — зафиксировать расхождение, оно войдёт в `caveat`
- Если первоисточник недоступен — сообщить пользователю и работать с тем что есть

---

### Шаг 3 — Разбор

Структурировать собранный материал по четырём пунктам:

- **Что это** — краткое определение события или факта
- **Контекст** — исторические аналогии, предыдущие прецеденты
- **Что означает сейчас** — интерпретация для BTC
- **Важные оговорки** — риски, ограничения, противоречия источников

Это читаемый промежуточный артефакт. Из него питаются поля JSON:

| Пункт разбора | Поля JSON |
|--------------|-----------|
| Что это | `signal`, `cat`, `actor` |
| Контекст | `context` |
| Что означает сейчас | `dir`, `horizon`, `weight` |
| Важные оговорки | `caveat`, `data` |

---

### Шаг 4 — Кластеризация

Определить `theme` по таблице метаметок.
Определить `cluster` — найти существующий кластер с совпадающим `tension`.
Если совпадений нет — зафиксировать как потенциально новый кластер (создаётся при 2+ сигналах с общим `tension`).

Кластер назначается здесь. Связывание — на следующем шаге.

---

### Шаг 5 — Связывание

Найти кандидатов: все сигналы с тем же `theme`. Если кластер определён — добавить все сигналы того же кластера.

Для каждого кандидата проверить три критерия:

**`confirms`** — оба сигнала говорят одно и то же но через разные механизмы?
→ Если да: добавить в `confirms`

**`contradicts`** — `macro_implication` одного опровергает `macro_implication` другого?
→ Если да: добавить в `contradicts`

**`context_chain`** — без понимания сигнала X текущий сигнал теряет смысл?
→ Если да: добавить в `context_chain`

Пустое поле — норма если после проверки всех кандидатов критерий не выполнен ни разу.
Не добавлять связь только потому что сигналы близки по теме.

---

### Шаг 6 — Синтез

Выполняется только после завершения шага 5.

**`tension`** — сформулировать по конструкции «X vs Y» / «X несмотря на Y» / «X при условии что Y».
Не описание факта — противоречие между двумя силами.

| ❌ Плохо | ✅ Хорошо |
|---------|---------|
| «Strategy продолжает покупать BTC» | «Strategy наращивает долг для покупки BTC vs рынок ставит NAV-дисконт 0.83x» |
| «ETF показал отток» | «ETF-оттоки как поверхностное давление vs долгосрочные держатели покупают в 10x больше» |

**`macro_implication`** — ответить на вопрос «что это меняет для BTC структурно».
Не пересказ события — вывод об изменении в системе.

| ❌ Плохо | ✅ Хорошо |
|---------|---------|
| «Franklin подала заявку на ETF» | «Пассивный дивидендный поток как источник BTC-спроса — новая категория не зависящая от настроений рынка» |
| «Lightning достиг рекорда» | «BTC масштабируется как платёжная сеть и резервный актив одновременно — два трека роста не противоречат» |

**`narrative_role`** — выбрать одну роль:

| Роль | Критерий |
|------|---------|
| `trigger` | Первый сигнал нового кластера или резкий перелом |
| `complication` | Добавляет противоречие в существующий нарратив |
| `resolution` | Закрывает `tension` предыдущих сигналов |
| `background` | Структурный контекст без острого события |

---

### Шаг 7 — Проверка

Сигнал готов к оформлению только если:

- `tension` сформулирован по формуле (не описание факта)
- `macro_implication` отвечает на «что меняется структурно» (не пересказ)
- `narrative_role` выбрана и обоснована
- `links` — все три поля проверены по критериям шага 5

Если что-то не выполнено — вернуться к соответствующему шагу.

---

### Шаг 8 — Оформление

Собрать финальный JSON по полной схеме объекта сигнала.
Одновременно обновить `SIGNALS.md`.
`index.html` не трогать.

---

## Правило наполнения вкладок

Claude **не наполняет вкладки из собственных знаний**. Только из материалов пользователя.

1. Пользователь присылает материал
2. Claude структурирует по схеме разбора (шаг 3)
3. Переход к следующей теме — только после явного сигнала пользователя

---

## Правило версионного напоминания

При каждом обновлении CLAUDE.md Claude отслеживает накопленный прирост версии **в рамках одной сессии**.

Когда накопленный прирост достигает **+0.4** — напомнить пользователю:

> ⚠️ Версия CLAUDE.md изменилась на 0.4 за эту сессию (текущая: vX.X).
> Рекомендую обновить инструкции в системном промпте чтобы следующая сессия стартовала с актуальными правилами.

**Как считать:**
- Старт сессии: фиксируем версию из шапки файла
- Каждое обновление: прибавляем дельту
- Достигли +0.4 → напоминание → счётчик сбрасывается

**Пример:** сессия началась на v4.0 → обновили до v4.4 → напоминание → следующий порог v4.8.

---

## Изменение этого файла

1. Версия в шапке: +0.1 минор / +1.0 мажор
2. Обновить дату
3. Добавить строку в `CHANGELOG.md`
