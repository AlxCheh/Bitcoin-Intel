# Narrative Intelligence Engine Specification (NIES)

**Версия:** 1.0
**Класс документа:** логическая архитектурная спецификация (технологически-независимая)
**Опора:** BDKS v1.2 ([BDKS.md](BDKS.md)) — знание; BAMS v1.2 ([BAMS.md](BAMS.md)) — метод. Оба документа авторитетны и здесь не переопределяются.
**Нижний слой:** алгоритмы рассуждения НЕ определяются в NIES. Каноническая спецификация алгоритмов ядра уже существует — [ALGORITHM.md](ALGORITHM.md) (нарративный синтез) и алгоритм обработки сигнала в CLAUDE.md (Шаги 1–8). NIES ссылается на них как на действующий Reasoning-слой, а не откладывает в будущий документ.
**Характер:** reverse-engineered архитектура работающей системы. Расхождения реализации с архитектурным идеалом фиксируются явно как архитектурный долг (Часть XI), а не маскируются.

---

## Часть I. Назначение и границы

### Назначение

Narrative Intelligence Engine (NIE) — логическая система, преобразующая разрозненный поток наблюдений о Bitcoin в связный, объяснимый и проверяемый корпус аналитического знания: сигналы, их связи, кластеры противоречий и синтезированные нарративы.

NIE операционализирует методологию BAMS над знанием BDKS. Он не генерирует знание из ничего и не заменяет аналитика — он гарантирует, что каждый вывод прошёл через обязательные методологические шаги и остался прослеживаемым до исходных наблюдений.

### Что система делает

- Принимает материал о событии, отклоняет непроверяемое (BAMS Р1: сигнал без источника — слух);
- Проводит верификацию через независимые источники (BAMS Р6);
- Классифицирует наблюдение по таксономиям BDKS (Р4, Р5) и связывает с онтологией (Р3);
- Устанавливает связи с существующим корпусом: подтверждение, противоречие, контекст (BAMS Р7);
- Синтезирует интерпретационные конструкции: tension, macro_implication, роль в нарративе (BAMS Р8);
- Показывает рассуждение до фиксации результата (BAMS Р11 — принцип видимости);
- Ведёт производные базы сущностей и механизмов, прослеживаемые до сигналов;
- Собирает из корпуса сигналов нарративные карточки детерминированным алгоритмом выбора (ALGORITHM.md).

### Что система не делает

- Не прогнозирует цену (BDKS Р3, класс Price; BAMS Р14);
- Не анализирует объекты вне причинной связи с Bitcoin (BDKS Р1, правило R-10);
- Не выдаёт вывод без прослеживаемости до сигналов (BAMS Финальный раздел);
- Не заполняет пробелы данных обобщённым знанием модели (инвариант «отсутствие скрытого домысливания»).

---

## Часть II. Архитектурные принципы

| Принцип | Воплощение в NIE | Источник требования |
|---|---|---|
| Single Source of Truth | Домен — только BDKS; метод — только BAMS; корпус сигналов — единственная база записей; производные базы ссылаются, не копируют | BDKS Р15, R-03 |
| Explainability | Интерпретационные решения показываются до фиксации; объяснение — не приложение к выводу, а способ его сборки | BAMS Р11 |
| Traceability | Каждый вывод → сигналы; каждый сигнал → источник; каждая запись производной базы → сигналы; id неизменяемы | BAMS Финальный; CLAUDE.md (правило неизменности id) |
| Determinism | Метод применяется одинаково к одинаковому входу; пакетный синтез нарративов — чистая функция от корпуса | BAMS Финальный (детерминированность метода) |
| Modularity | Подсистемы с непересекающимися зонами ответственности (Часть V) | Настоящий документ |
| Separation of Concerns | Три уровня: знание / метод / реализация — независимая эволюция каждого | BDKS Преамбула |
| Technology Independence | NIES не ссылается на языки, форматы файлов, конкретные хранилища | Настоящий документ |
| Extensibility | Открытые таксономии (кластеры, типы механизмов) с явной процедурой расширения | BDKS Финальный раздел |
| Reproducibility | Повторный прогон метода на том же корпусе даёт тот же результат | BAMS Р1 (воспроизводимость) |
| Auditability | История изменений знания, метода и корпуса ведётся с обоснованиями | BDKS Финальный, R-07 |

---

## Часть III. Контекст системы

### Внешние акторы

| Актор | Роль во взаимодействии |
|---|---|
| **Оператор-аналитик** (владелец домена + заказчик) | Поставляет материал; подтверждает интерпретационные решения; принимает изменения знания и метода (BAMS: модель ответственности) |
| **Исполнитель метода** | Человек или система, ведущая сигнал через пайплайн; в текущей реализации роли исполнителя и движка совмещены |
| **Внешние источники наблюдений** | Первоисточники и вторичные источники по типам сигналов (BDKS Р10 — шкала надёжности) |
| **Читатель** | Потребитель синтезированных нарративов и корпуса; не влияет на содержание |

### Входы системы (граница)

- Материал о событии: текст, ссылка, изображение — в любом виде, пригодном для фиксации «что именно поступило»;
- Результаты верификации из внешних источников;
- Решения оператора: подтверждение/корректировка интерпретаций, приёмка изменений.

### Выходы системы (граница)

- Записанный сигнал (структурированная запись + читаемый архив);
- Сводка решения (компактное резюме уже показанного рассуждения);
- Обновления производных баз (сущности, доходные механизмы);
- Нарративные карточки (пакетный синтез);
- Записи аудита: версии, обоснования изменений.

### Каталог требований (Стадия 1 — свод)

| ID | Требование | Источник |
|---|---|---|
| RQ-01 | Отклонять материал без проверяемого источника | BAMS Р1 |
| RQ-02 | Верифицировать через ≥1 независимый источник; фиксировать расхождения | BAMS Р6; CLAUDE.md Шаг 2 |
| RQ-03 | Различать независимые и зависимые источники до агрегирования | BAMS Р6 (двойной учёт) |
| RQ-04 | Классифицировать по таксономиям BDKS без их переопределения | BDKS Р4, Р5; R-03 |
| RQ-05 | Расщеплять достоверность факта и атрибуции для on-chain наблюдений | R-11 |
| RQ-06 | Рассматривать ≥2 конкурирующих объяснения до выбора | BAMS Р5 |
| RQ-07 | Применять тест настоящего противоречия до маркировки contradicts | BAMS Р7 |
| RQ-08 | Формулировать tension как конфликт механизмов, не описание факта | BAMS Р8; CLAUDE.md Шаг 6 |
| RQ-09 | Показывать интерпретационные решения до фиксации | BAMS Р11; CLAUDE.md (правило видимости) |
| RQ-10 | Сопровождать вывод ограничениями (caveat) и явной оценкой достоверности | BAMS Р9, Р10 |
| RQ-11 | Гарантировать неизменность идентификаторов после записи | CLAUDE.md (правило id) |
| RQ-12 | Вести производные базы с обратными ссылками на сигналы | CLAUDE.md (ENTITIES); расширение — REVENUE_ENGINES |
| RQ-13 | Собирать нарративные карточки детерминированно из полей корпуса | ALGORITHM.md |
| RQ-14 | Валидировать запись до фиксации (целостность, ссылки, канон меток) | CLAUDE.md Шаг 7–8 |
| RQ-15 | Изменения знания/метода — с обоснованием и проверкой на прецедентах | R-07, R-08; BAMS Р13 |
| RQ-16 | Признание недостаточности данных — валидный исход, не сбой | BAMS Р7, Р10 |
| RQ-17 | Границы домена соблюдаются на входе: не-Bitcoin материал отклоняется | BDKS Р1; R-10 |
| RQ-18 | Пакетный синтез не генерирует текст — только выбирает из написанного | ALGORITHM.md (принцип выбора) |

---

## Часть IV. Логическая архитектура

### Обзор подсистем

```
                                ┌─────────────────────┐
 Материал ──► S1 Приём ──► S2 Верификация ──► S3 Разбор │  интерактивный
                │              │                 │      │  контур
                ▼              ▼                 ▼      │  (сигнал)
              [отказ]    S10 Доступ к знанию (BDKS/мосты)
                               │                 │
                        S4 Классификация ◄───────┘
                               │
                        S5 Связывание ◄──── Корпус сигналов
                               │
                        S6 Синтез интерпретаций
                               │
              S7 Объяснение (видимость) ──► Оператор ──► подтверждение
                               │
                        S8 Валидация
                               │
                        S9 Фиксация и трассировка ──► Корпус + производные базы
                                                          │
                              пакетный контур:            ▼
                        S11 Нарративный композитор ──► Карточки нарративов
                               ▲
                        S12 Управление изменениями (governance, поверх всего)
```

Два контура работы: **интерактивный** (обработка одного сигнала с человеком в петле, S1–S9) и **пакетный** (детерминированная сборка нарративов из корпуса, S11). S10 и S12 — сквозные сервисы.

---

## Часть V. Спецификации подсистем

Формат: назначение · ответственность · входы → выходы · зависимости · отказ · качество.

### S1 — Приём материала (Input Management)

- **Назначение:** зафиксировать поступившее наблюдение без интерпретации.
- **Ответственность:** определить форму материала; зафиксировать исходную точку; отклонить материал вне границ домена (RQ-17) и материал без перспективы верификации (RQ-01). Не наполнять наблюдение знанием системы — только материалом источника.
- **Входы → выходы:** сырой материал → зафиксированное наблюдение ∨ мотивированный отказ.
- **Зависимости:** S10 (проверка границ домена по BDKS Р1).
- **Отказ:** материал вне домена; материал-слух. Отказ — валидный исход с объяснением.
- **Качество:** нулевая интерпретация на этом шаге; фиксация дословна.

### S2 — Верификация (Research & Source Verification)

- **Назначение:** установить достоверность наблюдения через независимые источники.
- **Ответственность:** найти первоисточник; привлечь дополнительные источники по типу сигнала (карта источников — BDKS Р10); проверить независимость (RQ-03); зафиксировать расхождения; определить дату события по приоритету первоисточника, не по формулировкам оператора.
- **Входы → выходы:** зафиксированное наблюдение → верифицированное наблюдение с картой источников и расхождений.
- **Зависимости:** внешние источники; S10 (шкала надёжности).
- **Отказ:** первоисточник недоступен → работа продолжается с явной пометкой; источники противоречат в ключевом факте → расхождение обязано попасть в ограничения (RQ-10).
- **Качество:** для on-chain — раздельная оценка факта и атрибуции (RQ-05).

### S3 — Разбор (Signal Decomposition)

- **Назначение:** структурировать верифицированное наблюдение в читаемый промежуточный артефакт.
- **Ответственность:** четыре грани разбора — что это / контекст / что означает сейчас / оговорки. Здесь же — обязательное рассмотрение альтернативных объяснений (RQ-06): выбранная интерпретация и хотя бы одна отклонённая.
- **Входы → выходы:** верифицированное наблюдение → интерпретационный разбор.
- **Зависимости:** S10 (онтология задаёт пространство объяснений — BAMS Р5).
- **Отказ:** невозможность интерпретации при текущих данных → явное признание недостаточности (RQ-16).
- **Качество:** каждая грань разбора питает конкретные поля записи (детерминированный маппинг — уровень реализации).

### S4 — Классификация и кластеризация (Classification & Clustering)

- **Назначение:** поместить наблюдение в таксономии и кластеры корпуса.
- **Ответственность:** тема, категория, актор, поток, горизонт, достоверность — строго по таксономиям BDKS через мост знание↔реализация; кластер — по двум признакам одновременно (тема + субъект действия); решение о новом кластере — по явному правилу порога.
- **Входы → выходы:** интерпретационный разбор → классифицированный сигнал.
- **Зависимости:** S10; корпус сигналов (существующие кластеры).
- **Отказ:** наблюдение не сопоставимо ни с одним классом онтологии → заявка на расширение BDKS через S12, не подгонка под ближайший класс (BAMS Р2).
- **Качество:** канонические метки без вариаций; классификация не использует конструкции синтеза (tension) как критерий.

### S5 — Связывание (Linking & Contradiction Manager)

- **Назначение:** установить связи нового сигнала с корпусом.
- **Ответственность:** отбор кандидатов (та же тема; тот же кластер); три независимых критерия — подтверждение (то же утверждение другим механизмом), противоречие (тест: могут ли оба быть верны одновременно — BAMS Р7), контекстная цепочка (без X сигнал теряет смысл). Пустая связь — норма. Запрет накрутки противоречий ради влияния на выбор нарратива.
- **Входы → выходы:** классифицированный сигнал + корпус → связанный сигнал.
- **Зависимости:** корпус сигналов; S10 (тест противоречия — канон BAMS Р7).
- **Отказ:** нет.
- **Качество:** каждая связь мотивирована пройденным критерием, показанным оператору (S7).

### S6 — Синтез интерпретаций (Interpretation Synthesizer)

- **Назначение:** сформулировать интерпретационные конструкции сигнала.
- **Ответственность:** tension (конфликт двух механизмов, не факт — RQ-08); macro_implication (структурное изменение, не пересказ); нарративная роль (по критериям: триггер / усложнение / разрешение / фон).
- **Входы → выходы:** связанный сигнал → синтезированный сигнал.
- **Зависимости:** S5 завершён (роль зависит от связей).
- **Отказ:** конструкции не проходят критерии формы → возврат на соответствующий шаг.
- **Качество:** нарратив следует порядку факт → механизм → гипотеза → доказательства → альтернативы → вывод (BAMS Р8); уверенность и ограничения не растворяются в тоне.

### S7 — Объяснение (Explanation Engine)

- **Назначение:** сделать рассуждение видимым до фиксации.
- **Ответственность:** показать оператору интерпретационные решения S3–S6 с обоснованиями до записи; свёртка процесса в готовый результат — нарушение метода, не стиль подачи (BAMS Р11). После фиксации — компактная сводка уже показанных решений.
- **Входы → выходы:** решения S3–S6 → показанное рассуждение → подтверждение оператора.
- **Зависимости:** оператор в петле.
- **Отказ:** оператор корректирует → возврат на затронутый шаг.
- **Качество:** полный набор элементов объяснимости (цепочка, доказательства, отвергнутые альтернативы, обоснование выбора, уверенность, ограничения) — не подмножество по выбору.

### S8 — Валидация (Validation Engine)

- **Назначение:** проверить готовность записи до фиксации.
- **Ответственность:** структурная целостность записи; уникальность и формат идентификатора; соответствие меток канону; заполненность обязательных полей; целостность ссылок (все связи указывают на существующие записи; производные базы — на существующие сигналы и сущности).
- **Входы → выходы:** синтезированный сигнал → валидированный сигнал ∨ список дефектов.
- **Зависимости:** корпус (проверка ссылок); автоматизированный контур проверки при интеграции изменений.
- **Отказ:** дефект → возврат с указанием шага.
- **Качество:** валидация детерминирована и автоматизируема.

### S9 — Фиксация и трассировка (Persistence & Traceability Manager)

- **Назначение:** записать сигнал и поддержать прослеживаемость корпуса.
- **Ответственность:** атомарная запись в корпус (структурированная запись + читаемый архив — одно изменение); присвоение неизменяемого идентификатора; обновление производных баз (сущности — участники события; доходные механизмы — при относимости) с обратными ссылками; журнал изменения.
- **Входы → выходы:** валидированный сигнал → записанный сигнал + обновлённые производные базы.
- **Зависимости:** S8 пройдена.
- **Отказ:** конфликт идентификатора → пересчёт номера, повторная валидация.
- **Качество:** после записи идентификатор не меняется никогда (RQ-11); расхождение префикса и уточнённой темы не исправляется задним числом.

### S10 — Доступ к знанию (Knowledge Access)

- **Назначение:** единая точка обращения к знанию домена и мостам.
- **Ответственность:** предоставлять таксономии, онтологию, карту источников, правила BDKS по ссылке; мост знание↔реализация (соответствие полей записей классам знания); запрет локальных переопределений (R-03).
- **Входы → выходы:** запрос подсистемы → каноническое знание + применимые правила.
- **Зависимости:** BDKS (версионированная опора), словарь данных.
- **Отказ:** знание отсутствует → заявка на эволюцию BDKS через S12, не локальная заплатка.
- **Качество:** фиксация версии знания, на которую опирается корпус.

### S11 — Нарративный композитор (Narrative Composer, пакетный контур)

- **Назначение:** собрать из корпуса карточки главных нарративов.
- **Ответственность:** детерминированное ранжирование сигналов кластера; выбор якорного сигнала; сборка карточки из уже написанных полей — композитор выбирает лучшее, не генерирует новое (RQ-18); правило смены главного противоречия кластера — только через честный тест связывания, не через накрутку.
- **Входы → выходы:** корпус сигналов → карточки нарративов.
- **Зависимости:** корпус; спецификация алгоритма ранжирования — ALGORITHM.md (Reasoning-слой, вне NIES).
- **Отказ:** кластер без синтезированных конструкций → карточка не собирается, кластер помечается.
- **Качество:** чистая функция от корпуса — повторный прогон без изменения корпуса даёт тот же результат.

### S12 — Управление изменениями (Governance Controller)

- **Назначение:** управлять эволюцией знания, метода и архитектуры.
- **Ответственность:** изменения знания — по процедуре BDKS (обоснование недостаточности прежней формулировки, R-07; порог пересмотра, R-08); изменения метода — с проверкой на исторических прецедентах до принятия (BAMS Р13); версионирование документов и инструкций с журналом; модель ответственности (владелец домена / исполнитель метода / реализация); защита целостности: изменения корпуса и производных баз проходят автоматизированную проверку до интеграции.
- **Входы → выходы:** предложение изменения → принятое изменение с обоснованием ∨ отклонение.
- **Зависимости:** все подсистемы (сквозной сервис).
- **Отказ:** изменение без обоснования не принимается; регресс на прецедентах — основание отклонения.
- **Качество:** направление синхронизации неизменно — производное приводится к источнику.

---

## Часть VI. Информационные потоки

### Артефакты пайплайна (интерактивный контур)

| # | Артефакт | Создаёт | Потребляет | Состояние |
|---|---|---|---|---|
| A1 | Зафиксированное наблюдение | S1 | S2 | сырьё |
| A2 | Верифицированное наблюдение + карта источников | S2 | S3 | проверено |
| A3 | Интерпретационный разбор (4 грани + альтернативы) | S3 | S4, S7 | интерпретировано |
| A4 | Классифицированный сигнал | S4 | S5, S7 | классифицировано |
| A5 | Связанный сигнал | S5 | S6, S7 | связано |
| A6 | Синтезированный сигнал | S6 | S7, S8 | синтезировано |
| A7 | Показанное рассуждение + подтверждение | S7 | S8 | одобрено |
| A8 | Валидированный сигнал | S8 | S9 | готово к записи |
| A9 | Записанный сигнал + обновления производных баз + сводка | S9 | корпус, S11, читатель | зафиксировано |

### Точки решения и циклы

- **D1 (S1):** в домене и проверяемо? — нет → отказ, конец.
- **D2 (S2):** источники согласованы? — нет → расхождение в ограничения, продолжить.
- **D3 (S4):** класс онтологии найден? — нет → заявка в S12, стоп до решения владельца домена.
- **D4 (S7):** оператор подтвердил? — нет → возврат на затронутый шаг (цикл).
- **D5 (S8):** валидация чиста? — нет → возврат (цикл).
- **Синхронизация:** S9 — единственная точка изменения корпуса; S11 читает корпус только после завершённой фиксации.

### Пакетный контур

Корпус (A9…) → S11 → карточки нарративов. Обратной связи в корпус нет — композитор только читает. Единственный канал влияния на карточки — изменение полей сигналов через полный интерактивный контур.

---

## Часть VII. Модель жизненного цикла

### Жизненный цикл сигнала

```
предложен → верифицирован → интерпретирован → классифицирован →
связан → синтезирован → показан → валидирован → записан → [неизменяемое ядро]
```

После записи неизменяемы: идентификатор, дата, исходный источник. Изменяемы через полный повторный проход соответствующих шагов: связи (при появлении новых сигналов корпуса), синтезированные конструкции (пересмотр tension — легитимный путь смены нарратива кластера). Обратимость вывода — инвариант BAMS: новый сигнал способен ослабить существующий вывод.

### Жизненный цикл кластера

```
нет → первый сигнал (триггер) → накопление (усложнения/фон) →
   [опционально] разрешение противоречия → зрелость / угасание
```

Кластер создаётся при пороге общности противоречия; главный tension кластера определяется композитором по правилу ранжирования (Reasoning-слой).

### Жизненный цикл записи производной базы

```
сущность/механизм упомянут как участник события → создание записи →
обновления при каждом относящемся сигнале (+обратная ссылка) → статус: активен/закрыт
```

Закрытие — состояние, не удаление: запись остаётся прослеживаемой.

### Жизненный цикл знания и метода

Управляется S12 по процедурам BDKS/BAMS: версия, обоснование, проверка на прецедентах, синхронизация производных. Мажорное изменение знания не применяется к производным автоматически (BDKS: обратная совместимость).

### Режимы работы

| Режим | Контур | Детерминизм | Человек в петле |
|---|---|---|---|
| Обработка сигнала | интерактивный | метод детерминирован, интерпретация подтверждается | да (обязателен) |
| Пакетный синтез нарративов | пакетный | полный | нет |
| Наполнение производных баз | интерактивный (в составе S9) | правила детерминированы | да |
| Эволюция знания/метода | governance | процедурный | да (владелец домена) |

---

## Часть VIII. Архитектурные контракты

Сводный контракт (полные обязанности — Часть V):

| Подсистема | Владеет | Читает | Производит | Запрещено |
|---|---|---|---|---|
| S1 | фиксация наблюдения | границы домена (S10) | A1 | интерпретировать; дополнять знанием системы |
| S2 | карта источников | внешние источники, шкала надёжности | A2 | подменять дату события датой сессии; агрегировать зависимые источники как независимые |
| S3 | интерпретационный разбор | A2, онтология | A3 | единственная гипотеза без альтернатив |
| S4 | классификация | A3, таксономии, корпус | A4 | локальные переопределения таксономий; использование tension как критерия |
| S5 | связи | A4, корпус | A5 | связь «по близости темы»; накрутка противоречий |
| S6 | интерпретационные конструкции | A5 | A6 | tension-описание факта; macro_implication-пересказ |
| S7 | видимость рассуждения | A3–A6 | A7 | свёртка процесса в готовый результат |
| S8 | вердикт валидации | A6/A7, корпус | A8 | пропуск записи с дефектом ссылочной целостности |
| S9 | корпус, производные базы | A8 | A9 | изменение id после записи; запись мимо валидации |
| S10 | доступ к знанию | BDKS, мосты | канон знания | переопределение знания |
| S11 | карточки нарративов | корпус | карточки | генерация текста; запись в корпус |
| S12 | версии, процедуры изменений | всё | принятые изменения | изменение без обоснования; регресс на прецедентах |

**Пред/постусловия ключевых переходов:** S6 начинается только после завершения S5 (роль зависит от связей); S8 — только после подтверждения в S7; S9 — только после чистой S8; S11 читает корпус только в зафиксированном состоянии.

---

## Часть IX. Атрибуты качества (измеримые)

| Атрибут | Требование | Проверка |
|---|---|---|
| Объяснимость | 100% записанных сигналов имели показанное рассуждение до фиксации | Аудит переписки/журнала против корпуса |
| Прослеживаемость | 100% связей и обратных ссылок производных баз разрешаются в существующие записи | Автоматизированная проверка ссылочной целостности |
| Согласованность | 0 расхождений меток с каноном; 0 дублей идентификаторов | Автоматизированная проверка |
| Аудируемость | 100% изменений знания/метода имеют запись с обоснованием | Журнал версий |
| Воспроизводимость | Пакетный синтез: два прогона на одном корпусе → идентичные карточки | Повторный прогон |
| Расширяемость | Новый тип механизма/кластера добавляется без изменения контрактов подсистем | Ревизия изменения |
| Надёжность | Отказ любой внешней зависимости не портит корпус (фиксация атомарна) | Ревизия точки S9 |
| Модульность | Ни одна ответственность не принадлежит двум подсистемам (Часть XI, проверка) | Архитектурная ревизия |
| Тестируемость | Валидация S8 полностью автоматизируема; изменения метода прогоняемы на прецедентах | Наличие автоматизированного контура |
| Технологическая независимость | В NIES отсутствуют упоминания технологий реализации | Ревизия документа |

---

## Часть X. Трассируемость

### BDKS → NIES

| BDKS | Элемент NIES |
|---|---|
| Р1 Границы; R-10 | S1 (D1 — фильтр домена); Часть I «что не делает» |
| Р2 Словарь | S10 (канон терминов) |
| Р3 Онтология; R-02 | S3 (пространство объяснений), S4 (классификация), S10 |
| Р4 События | S1–S2 (событие как датируемый факт), S4 |
| Р5 Таксономия сигналов; R-11 | S2 (расщепление достоверности), S4 |
| Р8 Причинные модели; R-01, R-04 | S3, S6 (механизм обязателен; аннотация петель) |
| Р9 Метрики; R-05 | S2–S3 (данные наблюдения); S12 (перекалибровка порогов) |
| Р10 Источники | S2 (карта источников, шкала надёжности) |
| Р11 Неопределённость; R-09 | S6–S7 (вероятностность выводов на эвристиках) |
| Р12 Искажения | S3 (альтернативы против confirmation bias), S5 (двойной учёт) |
| Р13 Golden Dataset; R-06 | S12 (валидация изменений метода) |
| Р14 Карта знаний | S6 (уровень интерпретации — зона метода); аннотация петель |
| Р15 Производные артефакты; R-03 | S10 (запрет переопределений); весь документ |
| Финальный (эволюция); R-07, R-08 | S12 |

### BAMS → NIES

| BAMS | Элемент NIES |
|---|---|
| Р1 Философия (событие/сигнал/вывод) | S1 (отклонение слухов), Часть I |
| Р3 Оси качества сигнала | S2 (верифицируемость, независимость, свежесть), S4 (значимость актора) |
| Р4 Причинность | S3, S6 (уровни причинности в разборе и синтезе) |
| Р5 Гипотезы (≥2) | S3 (обязательные альтернативы) — см. долг AD-2 |
| Р6 Доказательства | S2 (независимость, агрегирование) |
| Р7 Противоречия | S5 (тест настоящего противоречия; недостаточность — валидный исход) |
| Р8 Нарратив | S6 (обязательные элементы, порядок) |
| Р9 Уверенность | S6/S7 — см. долг AD-1 |
| Р10 Неопределённость | S2 (пробелы данных), S6 (ограничения) |
| Р11 Объяснимость | S7 (весь контракт подсистемы) |
| Р12 Искажения | распределены: S3, S5, S11 (механизмы снижения) |
| Р13 Golden Dataset (процедура) | S12 — см. долг AD-3 |
| Р14 Ограничения метода | Часть I «что не делает» |
| Финальный (инварианты) | Части II, VII (обратимость), VIII (контракты) |

---

## Часть XI. Отчёт архитектурной валидации

### Проверки

| Проверка | Результат |
|---|---|
| Полнота: каждый раздел BAMS отображён в ≥1 подсистему | ✓ (Часть X) |
| Полнота: каждое правило BDKS R-01…R-12 имеет точку применения | ✓ (R-02 — внутреннее правило онтологии, применяется в S12 при расширении знания) |
| Отсутствие дублей ответственности | ✓ — единственная точка записи корпуса (S9), единственная точка канона знания (S10), единственный владелец связей (S5) |
| Чистота границ | ✓ — интерпретация (S3–S6) отделена от фиксации (S9) и от чтения (S11) |
| Связность/зацепление | Высокая связность внутри подсистем; зацепление через артефакты A1–A9, не через общий внутренний state |
| Технологическая независимость | ✓ — документ не ссылается на реализацию |
| Готовность к реализации | Система уже реализована; NIES ей соответствует с оговорками ниже |

### Архитектурный долг (расхождения реализации с идеалом)

| ID | Долг | Идеал (источник) | Текущее состояние | Возможное направление |
|---|---|---|---|---|
| AD-1 | Явная трёхуровневая уверенность | BAMS Р9: высокая/средняя/низкая с операциональными критериями | Уверенность проксируется полями достоверности источника и ограничений; отдельного поля уровня уверенности в записи сигнала нет | Ввести поле уверенности с критериями BAMS Р9 при следующем мажорном изменении схемы |
| AD-2 | Формальный след отвергнутых гипотез | BAMS Р5, Р11: отвергнутые альтернативы — обязательный элемент объяснения | Альтернативы показываются в интерактивном рассуждении (S7), но не сохраняются как поле записи — после фиксации след остаётся только в переписке | Поле «рассмотренные альтернативы» либо журнал разборов |
| AD-3 | Автоматизированная регрессия изменений методологии на Golden Dataset | BAMS Р13: любое изменение метода прогоняется против набора исторических прецедентов до внедрения | Автоматизированная golden-регрессия существует для пакетного контура (детерминизм синтезатора), но не для аналитической методологии: изменения метода против прецедентов BDKS Р13 проверяются вручную | Контур регрессии методологии как расширение существующего golden-контура |
| AD-4 | Формальные альтернативные сценарии в нарративе | BAMS Р8: при уверенности ниже высокой — полноценный альтернативный сценарий | Карточка нарратива не содержит выделенного альтернативного сценария | Поле альтернативного сценария в синтезе кластера |
| AD-5 | Совмещение ролей | BAMS: модель ответственности различает исполнителя метода и реализацию | Роли совмещены в одной системе; разделение зон соблюдается процедурно, не структурно | Приемлемо при текущем масштабе; фиксировать в изменениях |

Долг зафиксирован сознательно: NIES описывает работающую систему честно, не рисуя несуществующие механизмы. Погашение каждого пункта — через процедуру S12 (обоснование, версия, проверка).

### Вердикт

Логическая архитектура полна относительно BAMS v1.2, прослеживаема до BDKS v1.2, свободна от дублей ответственности и технологических привязок. Пять пунктов архитектурного долга зафиксированы явно и не блокируют действующую эксплуатацию. NIES принимается как авторитетная архитектурная спецификация для последующих инженерных артефактов; алгоритмы рассуждения остаются в действующем Reasoning-слое (ALGORITHM.md; алгоритм обработки сигнала CLAUDE.md) и данным документом не переопределяются.

---

*Конец документа. Версия 1.0. Опора: BDKS v1.2, BAMS v1.2.*
