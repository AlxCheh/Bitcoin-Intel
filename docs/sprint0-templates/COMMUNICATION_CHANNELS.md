# Коммуникационные каналы и эскалация — черновик

> ⚠️ **ЧЕРНОВИК, не финальный документ.** Подготовлен как техническая заготовка
> для [GitHub Issue #79](https://github.com/AlxCheh/Bitcoin-Intel/issues/79).
> Реальные каналы (Slack? Telegram? только GitHub Issues?) не указаны
> намеренно — не факт архитектуры, а факт текущей практики команды,
> которого у меня нет. Заполнить и перенести в `docs/ONBOARDING.md`,
> когда каналы известны — этот файл удалить.

---

## Структура раздела (куда перенести после заполнения)

### Где обсуждать рутинные вопросы

`[канал не указан]` — например: «вопросы по конкретному сигналу — комментарий
в PR; вопросы по архитектуре — GitHub Discussions; срочное — Х»

### Куда эскалировать блокер

`[не указано]` — к кому обращаться, если задача застряла (заблокирована
чужим решением, недостающей информацией, конфликтом мнений).

Единственный уже существующий прецедент эскалации в проекте — правило
Sprint 0 Gate Decision (`docs/IRP_v1.md` §14): «При несогласии одного из
четырёх — Sprint 0 не открывается до устранения разногласия. Нет права
вето у одного человека, но нет и принципа большинства: каждая роль
покрывает свой домен, и разногласие означает незакрытый риск в этом
домене». Это специфично для одного решения (Gate), но сам принцип —
эскалация не разрешается голосованием, а требует закрытия домена — можно
взять как основу для общего правила эскалации, если организационно
подойдёт.

### Ожидаемый SLA на ответ

`[не указано]` — сколько времени разумно ждать реакции на блокер, прежде
чем эскалировать дальше. Отмечено как отдельный пробел и в
`docs/ERB_REPORT_v1.md` (Этап 9, Operations #71 — SLA не определены
явно нигде в проекте).

---

## Открытые вопросы для организационного решения

- Реальный канал(ы) — что уже используется на практике?
- Кто отвечает за Engineering Manager-роль из Issue #79 (ответственный
  за эту задачу по `docs/SPRINT0_PLAN.md`) — если роль ещё не занята,
  задача блокируется этим фактом, не техникой
